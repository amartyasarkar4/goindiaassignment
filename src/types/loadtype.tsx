export interface DisscussionSingleProp {
  author: string;
  photo: string;
  sector: string;
  desc: string;
  likes: string;
  view: string;
  comments: string;
  time: Date;
}

export interface MarketStorySingleProp {
  heading: string;
  photo: string;
  description: string;
}
